import numpy as np

def preprocess_data(data):
    # Example processing; adjust based on your data format
    processed_data = np.array([data['property1'], data['property2'], data['property3']])
    return processed_data
